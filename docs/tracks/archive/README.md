# Track archive

Historical per-track status files that used to live at repo root
(`TRACK-A-DONE.md` … `TRACK-N-DONE.md`, plus Track 2/3 progress / ready notes
and the orphaned `RESUME-TRACK-1.md`).

Moved here 2026-04-21 to stop polluting the repo root listing. Kept for
`git blame` / narrative context; do not add new track files here — use
`docs/issues/` for active bugs and `plans/` (gitignored) for in-flight work.
