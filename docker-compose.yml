# allhands — one-command self-deploy.
# Services: backend (FastAPI), web (Next.js).
#
# Self-instrumented · the platform writes its own trace events to the local
# SQLite DB and renders them via /observatory; no external tracing backend
# needed. Langfuse + the embedded Postgres were removed in 2026-04-25.
#
# Data:
#   - SQLite DB + checkpoint DB live under ./data (bind-mounted).
#
# Defaults are dev-friendly. For prod, set ALLHANDS_* env vars + strong secrets.

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: allhands-backend:dev
    environment:
      ALLHANDS_ENV: ${ALLHANDS_ENV:-dev}
      ALLHANDS_DATABASE_URL: sqlite+aiosqlite:////app/data/allhands.db
      ALLHANDS_CHECKPOINT_DB_PATH: /app/data/checkpoints.db
      ALLHANDS_OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ALLHANDS_OPENAI_BASE_URL: ${OPENAI_BASE_URL:-}
      ALLHANDS_DEFAULT_MODEL_REF: ${DEFAULT_MODEL_REF:-openai/gpt-4o-mini}
      ALLHANDS_CORS_ALLOW_ORIGINS: '["http://localhost:3000"]'
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health').read()"]
      interval: 10s
      timeout: 3s
      retries: 5

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    image: allhands-web:dev
    environment:
      BACKEND_ORIGIN: http://backend:8000
      NODE_ENV: production
    ports:
      - "3000:3000"
    depends_on:
      - backend
